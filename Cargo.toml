[workspace]
members = [
    "services/risk-analytics",
    "services/compliance",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
overflow-checks = false
