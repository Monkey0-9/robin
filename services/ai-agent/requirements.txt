# Robin Trading Platform — Python Dependencies
# ============================================================
# Install: pip install -r requirements.txt
# For CUDA llama-cpp: see scripts/download_models.sh
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu122
# ============================================================

# ─── API / Serving ───────────────────────────────────────────
fastapi==0.103.1
uvicorn==0.23.2
pydantic==2.3.0

# ─── Data acquisition ────────────────────────────────────────
yfinance>=0.2.40           # Yahoo Finance historical data (100yr depth)
pandas-datareader>=0.10.0  # FRED macro data (Fed Funds, CPI, VIX)
requests>=2.32.0
lxml>=5.1.0

# ─── Data processing ─────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=14.0.0            # Parquet I/O with ZSTD compression
fastparquet>=2024.2.0
zstandard>=0.22.0

# ─── Machine learning ────────────────────────────────────────
scikit-learn>=1.4.0        # Ridge regression, TimeSeriesSplit walk-forward CV
scipy>=1.12.0

# ─── AI inference ────────────────────────────────────────────
# llama-cpp-python: install separately with CUDA (see above)
llama-cpp-python
onnxruntime>=1.17.0        # CPU inference for FinBERT ONNX
transformers>=4.40.0       # FinBERT tokenizer
huggingface-hub>=0.22.0    # Model download helper

# ─── Live feed ───────────────────────────────────────────────
websockets>=12.0           # Binance/Alpaca WebSocket streams
pyzmq>=25.1.0              # ZeroMQ signal bus

# ─── Backtesting / strategy ──────────────────────────────────
matplotlib>=3.8.0          # Equity curve plots
seaborn>=0.13.0

# ─── Security ────────────────────────────────────────────────
cryptography>=42.0.0

# ─── Utilities ───────────────────────────────────────────────
python-dotenv==1.0.0
tqdm>=4.66.0
tabulate>=0.9.0
colorlog>=6.8.0
